import { module, test } from 'qunit';
import { visit, currentURL } from '@ember/test-helpers';
import { setupApplicationTest } from 'ember-qunit';

module('Acceptance | get current version', function(hooks) {
  // setupApplicationTest(hooks);
  //
  // test('visiting /get-current-version', async function(assert) {
  //   await visit('/get-current-version');
  //
  //   assert.equal(currentURL(), '/get-current-version');
  // });
});
